import _ from 'lodash';
import {FIELDS} from './fields';

export function onChange(values, dispatch, props, previousValues) {

    const filterValues = {...values};

    _.each(filterValues, (value, key) => {


    });

    //props.onSubmit(filterValues);
}